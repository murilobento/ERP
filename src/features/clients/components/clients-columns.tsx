import { createContactColumns } from '@/features/shared/contact-columns'
import { DataTableRowActions } from './data-table-row-actions'

export const clientsColumns = createContactColumns(DataTableRowActions)
